import { ReservationplatoRequest } from './reservationplato-request';

describe('ReservationplatoRequest', () => {
  it('should create an instance', () => {
    expect(new ReservationplatoRequest()).toBeTruthy();
  });
});
